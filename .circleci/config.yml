version: 2
jobs:
  build:
    docker:
      - image: circleci/openjdk:8-jdk

    working_directory: ~/dmn-tester

    environment:
      MAVEN_OPTS: -Xmx3200m

    steps:
      - checkout
      - run: cd ./server && mvn test